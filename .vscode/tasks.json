{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "rebuild hrp service",
            "command": "docker compose -f ./.docker/compose.yml up -d --no-deps --build hrp"
        }
    ]
}